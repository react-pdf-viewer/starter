{
    "name": "react-pdf-viewer-nextjs",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "copy": "rm -rf public && mkdir public && cp ../assets/pdf-open-parameters.pdf public",
        "dev": "npm run copy && next dev",
        "build": "npm run copy && next build",
        "start": "npm run copy && next start"
    },
    "dependencies": {
        "@react-pdf-viewer/core": "3.6.0",
        "@react-pdf-viewer/default-layout": "3.6.0",
        "next": "^12.2.3",
        "pdfjs-dist": "^2.15.349",
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    }
}
