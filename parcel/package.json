{
    "name": "react-pdf-viewer-parcel",
    "private": true,
    "version": "0.0.0",
    "source": "src/index.html",
    "scripts": {
        "copy": "rm -rf dist && mkdir dist && cp ../assets/pdf-open-parameters.pdf dist",
        "dev": "npm run copy && parcel",
        "build": "npm run copy && parcel build"
    },
    "dependencies": {
        "@react-pdf-viewer/core": "3.0.0",
        "@react-pdf-viewer/default-layout": "3.0.0",
        "pdfjs-dist": "^2.12.313",
        "react": "^17.0.2",
        "react-dom": "^17.0.2"
    },
    "devDependencies": {
        "parcel": "^2.2.0"
    }
}
